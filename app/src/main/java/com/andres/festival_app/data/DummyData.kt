package com.andres.festival_app.data

import com.andres.festival_app.data.model.FestivalModel

// Datos de card por defecto.
val name = "Tomorrowland"
val place = "Belgium"

var festivals = mutableListOf(
    FestivalModel(name, place)
)